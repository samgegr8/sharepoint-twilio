import { Question, Answers } from 'inquirer';
import { IAuthContext, IAuthConfigSettings } from '../interfaces';
export declare const shouldSkipQuestionPromptMapper: <T = Question<Answers>>(questions: T[], authContext: IAuthContext, settings: IAuthConfigSettings, answersAll: Answers) => Promise<T[]>;
